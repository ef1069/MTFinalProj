# NOTE: Cloud Run is typically deployed via CLI (gcloud run deploy) rather than kubectl.
# This YAML is for reference only—use gcp/deploy-cloud-shell.sh or gcloud CLI commands.
#
# To deploy frontend:
# gcloud run deploy mt-bookclubs-frontend \
#   --image gcr.io/$PROJECT_ID/mt-bookclubs-frontend:latest \
#   --platform managed \
#   --region us-central1 \
#   --allow-unauthenticated \
#   --memory 256Mi \
#   --timeout 300 \
#   --set-env-vars VITE_API_URL="$BACKEND_URL"
